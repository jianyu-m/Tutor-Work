
#include "sequence.h"
#include <algorithm>
// template <typename T>
// istream& operator>>(istream& in, const Sequence<T>& seq) {
//   return in;
// }

// template <typename T>
// ostream& operator<<(ostream& out, const Sequence<T>& seq) {
//   return out;
// }

// template <typename T>
// bool seq_comp(Sequence<T>& a, Sequence<T>& b){
//  return true;
// }

// template <typename T>
// void sort_list(vector<Sequence<T>> &seq) {
//   sort(seq.begin(), seq.end(), seq_comp);
// }

// template <typename T>
// istream& operator>>(istream& in, Sequence<T>& seq) {
//
// }
